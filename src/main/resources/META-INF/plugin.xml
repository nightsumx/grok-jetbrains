<!-- Plugin Configuration File -->
<idea-plugin>
    <id>com.sum.grok.jetbrains</id>
    <name>Grok Build</name>
    <version>0.1.1</version>
    <vendor email="lineryforjs@gmail.com" url="https://github.com/nightsumx/grok-jetbrains">lineryforjs</vendor>
    <idea-version since-build="242" until-build="261.*"/>

    <description><![CDATA[
      <blockquote>
        <p><b>Requires</b> the
        <a href="https://docs.x.ai/docs">Grok Build CLI</a>
        installed separately (<code>grok</code> on your PATH, or set the path under
        <b>Settings → Tools → Grok Build</b>).</p>
      </blockquote>

      <p><b>Grok Build</b> for JetBrains brings the coding agent into WebStorm, IntelliJ IDEA,
      PyCharm, GoLand, PhpStorm, and other IDEs — similar in spirit to the Claude Code bridge.</p>

      <h2>What you get</h2>
      <ul>
        <li><b>Open Grok</b> — <code>Ctrl+Esc</code> (⌃Esc) opens a dedicated Terminal tab and runs <code>grok</code>.</li>
        <li><b>Full-auto selection</b> — selecting code in the editor automatically tracks context
            and injects it into the Grok prompt when Grok is open (or when you open it).
            Manual re-send: <code>Ctrl+Alt+K</code> / <code>⌥⌘K</code>.</li>
        <li><b>IDE MCP bridge</b> — a local HTTP MCP server exposes open files, diagnostics, reformat,
            and diff tools. Auto-registers as <code>[mcp_servers.jetbrains]</code> in
            <code>~/.grok/config.toml</code>.</li>
        <li><b>Status bar</b> — shows the live MCP port (<code>Grok:12345</code>).</li>
      </ul>

      <h2>Shortcuts</h2>
      <ul>
        <li><code>Ctrl+Esc</code> — Open / focus Grok Build terminal</li>
        <li><code>Ctrl+Alt+K</code> / <code>⌥⌘K</code> — Send selection into Grok prompt</li>
      </ul>

      <h2>Requirements</h2>
      <ul>
        <li>JetBrains IDE 2024.2+</li>
        <li>Grok Build CLI authenticated on this machine</li>
        <li>Terminal plugin (bundled in WebStorm)</li>
      </ul>

      <p>Open source (Apache-2.0):
      <a href="https://github.com/nightsumx/grok-jetbrains">github.com/nightsumx/grok-jetbrains</a><br/>
      <i>Not affiliated with xAI.</i></p>
    ]]></description>

    <change-notes><![CDATA[
      <b>0.1.1</b>
      <ul>
        <li><b>Full-auto selection inject</b> — no hotkey required: selection is tracked continuously
            and pasted into the Grok prompt when Grok opens/focuses or when the selection changes
            while Grok is already open</li>
        <li>Settings toggles for auto-inject / inject-on-focus / inject-on-change</li>
        <li>Persists <code>~/.grok/ide/last-selection.md</code> + <code>selection.json</code></li>
      </ul>
      <b>0.1.0</b>
      <ul>
        <li>Open Grok Build in the IDE terminal (<code>Ctrl+Esc</code>)</li>
        <li>Send selection hotkey + local IDE MCP bridge</li>
      </ul>
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>
    <depends optional="true" config-file="plugin-terminal.xml">org.jetbrains.plugins.terminal</depends>

    <resource-bundle>messages.GrokBundle</resource-bundle>

    <extensions defaultExtensionNs="com.intellij">
        <postStartupActivity implementation="com.xai.grok.jetbrains.GrokStartup"/>
        <applicationService serviceImplementation="com.xai.grok.jetbrains.settings.GrokSettings"/>
        <applicationService serviceImplementation="com.xai.grok.jetbrains.services.IdeBridgeService"/>
        <projectService serviceImplementation="com.xai.grok.jetbrains.services.SelectionTracker"/>
        <projectService serviceImplementation="com.xai.grok.jetbrains.services.SelectionAutoInject"/>
        <applicationConfigurable
                parentId="tools"
                instance="com.xai.grok.jetbrains.settings.GrokConfigurable"
                id="com.sum.grok.jetbrains.settings"
                displayName="Grok Build"
                dynamic="true"/>
        <notificationGroup id="GrokBuild" displayType="BALLOON"/>
        <notificationGroup id="GrokBuildError" displayType="BALLOON"/>
        <statusBarWidgetFactory
                id="GrokBuildStatus"
                implementation="com.xai.grok.jetbrains.GrokStatusBarFactory"/>
    </extensions>

    <actions>
        <action id="com.sum.grok.jetbrains.OpenGrok"
                class="com.xai.grok.jetbrains.actions.OpenGrokInTerminalAction"
                text="Grok Build"
                description="Open Grok Build in the built-in terminal"
                icon="/icons/grok.svg">
            <keyboard-shortcut keymap="$default" first-keystroke="ctrl ESCAPE"/>
            <add-to-group group-id="MainToolbarRight" anchor="last"/>
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
        <action id="com.sum.grok.jetbrains.SendToGrok"
                class="com.xai.grok.jetbrains.actions.SendToGrokAction"
                text="Send to Grok Build"
                description="Inject the current selection into the Grok prompt"
                icon="/icons/grok.svg">
            <keyboard-shortcut keymap="$default" first-keystroke="alt ctrl K"/>
            <keyboard-shortcut keymap="Mac OS X" first-keystroke="meta alt K"/>
            <keyboard-shortcut keymap="Mac OS X 10.5+" first-keystroke="meta alt K"/>
            <add-to-group group-id="EditorPopupMenu" anchor="last"/>
            <add-to-group group-id="Floating.CodeToolbar" anchor="last"/>
        </action>
    </actions>
</idea-plugin>
